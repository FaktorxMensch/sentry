import "../../Assets/css/Settings/settingsElement.css";
import React from "react";

const SettingsElement = ({ children }) => (
  <div className="settingsElement">{children}</div>
);

export default SettingsElement;

import "../../Assets/css/Settings/settingsContent.css";
import React from "react";

const SettingsContent = ({ children }) => (
  <div className="settingsContent">{children}</div>
);

export default SettingsContent;
